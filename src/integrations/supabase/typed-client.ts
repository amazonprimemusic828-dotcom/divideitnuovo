// Compatibility import: never create a second client or auth session.
export { supabase, default, SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_PUBLISHABLE_KEY } from './client';
export type { Database } from './client';
