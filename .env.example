# Frontend (Vite) — safe to expose (anon/publishable keys only)
# Copy to .env.local and set the public key from the project below.
VITE_SUPABASE_URL="https://gsdkunlnjirimxcoqwxd.supabase.co"
VITE_SUPABASE_ANON_KEY=""

# ============================================================
# Edge function secrets — set via `supabase secrets set`,
# NEVER in this file and NEVER committed:
#
#   STRIPE_SECRET_KEY            (use a Restricted Key)
#   STRIPE_WEBHOOK_SECRET
#   SUPABASE_SERVICE_ROLE_KEY    (auto-provided by Supabase)
#   CREDENTIALS_ENCRYPTION_KEY   (openssl rand -base64 32)
#   CRON_SECRET                  (openssl rand -base64 32 — protects hold-release)
#   ALLOWED_ORIGINS              (comma-separated extra CORS origins)
#   LOVABLE_API_KEY
#   TWILIO_API_KEY
#   TWILIO_FROM_NUMBER
#   FIREBASE_PROJECT_ID
# ============================================================
